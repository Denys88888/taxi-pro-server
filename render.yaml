services:
  - type: web
    name: taxi-pro-server
    env: node
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    envVars:
      - key: PI_API_KEY
        sync: false
      - key: PORT
        value: 10000
